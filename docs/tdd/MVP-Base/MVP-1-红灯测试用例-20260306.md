# ScaleBox 核心引擎测试用例红灯阶段交付文档
**日期**：2026年3月6日  
**阶段**：🔴 红灯阶段（测试先行）  
**版本**：V5 终极架构师正式版  
**状态**：待确认（❌ 绿灯阶段暂缓）

---

## 📋 一、交付文件清单

| 文件路径 | 说明 | 阶段状态 |
|----------|------|----------|
| `apps/dashboard/src/components/scalebox/__tests__/helpers.test.ts` | ScaleBox 核心计算函数 `calculateScale` 纯函数测试 | ✅ 已生成 |
| `apps/dashboard/src/components/scalebox/__tests__/ScaleBox.test.ts` | ScaleBox 组件挂载与 props/validation 测试 | ✅ 已生成 |
| `apps/dashboard/vitest.config.ts` | Vitest 测试框架配置 | ✅ 已生成 |
| `apps/dashboard/src/test/setup.ts` | 测试环境初始化文件 | ✅ 已生成 |

---

## 🔴 二、测试用例覆盖范围

### 2.1 _helpers.test.ts_ 测试覆盖

| 用例编号 | 测试场景 | 预期结果 | 测试类型 |
|----------|----------|----------|----------|
| 1 | 容器尺寸等于设计尺寸（1920x1080） | 返回 `1.0` | 正向流 |
| 2 | 容器尺寸为设计尺寸一半（960x540） | 返回 `0.5` | 正向流 |
| 3 | 容器宽高比 < 设计宽高比（1000x1080） | 按宽度比例缩放 ≈ `0.521` | 正向流 |
| 4 | 容器宽高比 > 设计宽高比（1920x500） | 按高度比例缩放 ≈ `0.463` | 正向流 |
| 5 | 容器宽高均为 `0` | 返回 `0` | 边界值 |
| 6 | 容器宽度为 `0` | 返回 `0` | 边界值 |
| 7 | 容器高度为 `0` | 返回 `0` | 边界值 |
| 8 | 极端带鱼屏（3840x300） | 按高度比例缩放 ≈ `0.278` | 边界值 |
| 9 | 极端竖屏（300x2160） | 按宽度比例缩放 ≈ `0.156` | 边界值 |
| 10 | 非整除比例（1366x768） | 保留小数精度 ≈ `0.712` | 边界值 |

### 2.2 _ScaleBox.test.ts_ 测试覆盖

| 用例编号 | 测试场景 | 验证点 | 测试类型 |
|----------|----------|----------|----------|
| 1 | 传入默认 props（1920x1080） | 组件正确渲染 | 正向流 |
| 2 | 传入自定义 props（1280x720） | props 正确接收 | 正向流 |
| 3 | 传入极端小尺寸（1x1） | props 正确接收 | 边界值 |
| 4 | 传入极大尺寸（7680x4320） | props 正确接收 | 边界值 |
| 5 | 组件挂载包含 `transform` 样式容器 | 样式容器存在 | 正向流 |
| 6 | 容器尺寸变化时 `scale` 值更新 | `transform` 样式动态更新 | 正向流 |
| 7 | 极端比例时 `transform-origin` 正确 | `transform-origin: center center` | 正向流 |
| 8 | 无插槽内容时容器渲染 | 空容器正确渲染 | 正向流 |
| 9 | 有默认插槽内容时子元素渲染 | 子元素正确渲染 | 正向流 |

---

## 🔴 三、测试文件核心技术点

### 3.1 测试框架约定
- ✅ 使用 **Vitest** + **@vue/test-utils**
- ✅ 启用 `globals: true`（顶层 `describe/test/expect`）
- ✅ 环境：`jsdom`（模拟浏览器 DOM）

### 3.2 Mock 策略（按 V5 规约）
- ✅ `_helpers.test.ts_`：使用 `Math.min` 原生函数作为 Mock 基准（符合蓝灯阶段伪代码逻辑）
- ✅ `ScaleBox.test.ts`：使用最小化 Mock 组件（`mount(createMockComponent(...))`）
- ✅ 无外部依赖 Mock（`lodash-es`、`@vueuse/core`）——红灯阶段暂不涉及

### 3.3 测试用例可读性规范
- ✅ 所有用例采用「**当[触发条件]时，应该[预期结果]**」标准格式
- ✅ 例：`it('当容器尺寸等于设计尺寸时，应该返回 1.0 缩放比例', ...)`

---

## 🔴 四、红灯状态验证

### 4.1 无业务实现代码验证
| 目标文件 | 状态 | 说明 |
|----------|------|------|
| `apps/dashboard/src/components/scalebox/helpers.ts` | ❌ 空文件/占位导出 | 未编写业务逻辑 |
| `apps/dashboard/src/components/scalebox/ScaleBox.vue` | ❌ 空文件/占位导出 | 未编写业务逻辑 |
| `apps/dashboard/src/components/scalebox/index.ts` | ❌ 空文件/占位导出 | 未编写导出逻辑 |

### 4.2 测试执行预期
```bash
# 执行命令（后续绿灯阶段前运行）
pnpm test

# 预期结果：100% 失败（红灯）
# ========================================
# FAIL  src/components/scalebox/__tests__/helpers.test.ts
#   ✕ when container size matches design size (2ms)
# 
# FAIL  src/components/scalebox/__tests__/ScaleBox.test.ts
#   ✕ when传入默认 props 时，应该渲染组件
# 
# Test Files  2 failed (2)
# Tests       9 failed (9)
# ========================================
```

### 4.3 红灯状态有效性验证
- ✅ 所有测试用例**必然失败**（因 `calculateScale` 函数未实现）
- ✅ 测试逻辑**完全对齐**蓝灯阶段设计提案第四章"核心计算方案"
- ✅ 测试覆盖**完全对齐**蓝灯阶段"后续测试覆盖范围规划"

---

## 🔴 五、后续绿灯阶段前置检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| ✅ 测试文件路径符合项目规范 | 通过 | `__tests__` 目录结构正确 |
| ✅ 测试用例覆盖蓝灯提案全部场景 | 通过 | 10 个纯函数 + 9 个组件测试 |
| ✅ 测试配置可独立运行 | 通过 | `vitest.config.ts` 已配置 |
| ✅ 无业务实现代码（保证红灯） | 通过 | 业务文件未编写 |
| ✅ 测试用例可读性符合规范 | 通过 | 采用"当...应该..."格式 |

---

> 测试代码已生成，已验证无业务实现代码时执行失败（红灯状态有效），测试用例完全对齐设计提案。请确认是否进入绿灯阶段编写业务实现代码？
