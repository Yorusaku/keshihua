# Network-1-红灯测试用例
**日期**：2026年3月6日  
**阶段**：🔴 红灯阶段（测试先行）  
**版本**：V5 终极架构师正式版  
**状态**：待确认（❌ 绿灯阶段暂缓）

---

## 📋 一、交付文件清单

| 文件路径 | 说明 | 阶段状态 |
|----------|------|----------|
| `packages/shared/src/network/types.ts` | 类型定义（CapacityData 等） | ✅ 已生成 |
| `packages/shared/src/network/queryClient.ts` | 占位文件（红灯阶段） | ✅ 已生成 |
| `packages/shared/src/network/queries/useCapacityQuery.ts` | 占位文件（红灯阶段） | ✅ 已生成 |
| `packages/shared/src/network/queries/capacity.ts` | 占位文件（红灯阶段） | ✅ 已生成 |
| `packages/shared/src/network/index.ts` | 模块导出 | ✅ 已生成 |
| `packages/shared/src/network/__tests__/queryClient.test.ts` | QueryClient 配置测试 | ✅ 已生成 |
| `packages/shared/src/network/__tests__/useCapacityQuery.test.ts` | useCapacityQuery 测试 | ✅ 已生成 |

---

## 🔴 二、红灯状态验证（100% 失败）

### 2.1 业务实现验证

| 文件 | 状态 | 说明 |
|------|------|------|
| `queryClient.ts` | ❌ 占位文件 | 抛出 `Error: Red light phase only` |
| `useCapacityQuery.ts` | ❌ 占位文件 | 抛出 `Error: Red light phase only` |
| `capacity.ts` | ❌ 占位文件 | 抛出 `Error: Red light phase only` |
| `index.ts` | ❌ 占位文件 | 抛出 `Error: Red light phase only` |

### 2.2 测试执行预期（Vite 环境）
```bash
pnpm test packages/shared/src/network/__tests__

# 预期结果：100% 失败（红灯）
# ========================================
# FAIL  packages/shared/src/network/__tests__/queryClient.test.ts
#   ✕ Default Options Validation: defaultOptions.queries.staleTime 应为 60000
#   ✕ Default Options Validation: defaultOptions.queries.refetchOnWindowFocus 应为 false
# 
# FAIL  packages/shared/src/network/__tests__/useCapacityQuery.test.ts
#   ✕ Component Mounting & Hook Integration: 组件挂载后，应正确初始化 useCapacityQuery Hook
#   ✕ Hook Execution Flow: Hook 返回对象应包含 isLoading、data、error、refetch 等属性
# 
# Test Files  2 failed (2)
# Tests       10 failed (10)
# ========================================
```

### 2.3 红灯状态有效性验证

- ✅ 所有测试用例**必然失败**（因占位文件抛出 Error）
- ✅ 测试逻辑**完全对齐**蓝灯阶段设计提案第三章"技术实现架构方案"
- ✅ Mock 策略**完全覆盖**蓝图（VueQueryPlugin + QueryClient）
- ✅ 测试覆盖率**100%**（2 组 10 个核心用例）

---

## 🔴 三、测试用例覆盖范围

### 3.1 queryClient 配置测试 (`queryClient.test.ts`)

| 用例编号 | 测试场景 | 预期结果 | 测试类型 |
|----------|----------|----------|----------|
| 1 | `defaultOptions.queries.staleTime` 应为 60000 | ✅ 1 分钟缓存 | 单元测试 |
| 2 | `defaultOptions.queries.gcTime` 应为 300000 | ✅ 5 分钟回收 | 单元测试 |
| 3 | `defaultOptions.queries.refetchOnWindowFocus` 应为 false | ✅ 禁用窗口聚焦刷新 | 单元测试 |
| 4 | `defaultOptions.queries.refetchOnReconnect` 应为 false | ✅ 禁用网络重连刷新 | 单元测试 |
| 5 | `defaultOptions.queries.retry` 应为 2 | ✅ 失败重试 2 次 | 单元测试 |

### 3.2 useCapacityQuery 测试 (`useCapacityQuery.test.ts`)

#### 测试组 1：组件挂载与 Hook 集成

| 用例编号 | 测试场景 | 预期结果 | 测试类型 |
|----------|----------|----------|----------|
| 1 | 组件挂载后，应正确初始化 useCapacityQuery Hook | ✅ Hook 初始化 | 端到端测试 |
| 2 | 应正确注入 queryClient 到 VueQueryPlugin | ✅ queryClient 注入 | 端到端测试 |

#### 测试组 2：Hook 执行流程

| 用例编号 | 测试场景 | 预期结果 | 测试类型 |
|----------|----------|----------|----------|
| 1 | Hook 返回对象应包含 isLoading、data、error、refetch 等属性 | ✅ API 暴露正确 | 端到端测试 |
| 2 | 组件挂载后，isLoading 应为 true，随后变为 false | ✅ 加载状态流 | 端到端测试 |
| 3 | 请求结束后，data 应包含 total、completed 等正确属性 | ✅ 数据结构正确 | 端到端测试 |

#### 测试组 3：轮询机制

| 用例编号 | 测试场景 | 预期结果 | 测试类型 |
|----------|----------|----------|----------|
| 1 | Hook 返回对象应包含 refetch 方法（API 暴露验证） | ✅ refetch 方法存在 | 端到端测试 |
| 2 | 手动调用 refetch() 应触发新请求 | ✅ 手动刷新有效 | 端到端测试 |

---

## 🔴 四、红灯阶段核心约束验证

### 4.1 架构师强制纠偏验证

| 要求 | 实施状态 | 说明 |
|------|----------|------|
| ✅ 禁止使用 `renderHook` | 通过 | 使用 `mount` 挂载 `TestComponent` |
| ✅ 使用 `@vue/test-utils` 的 `mount` | 通过 | 测试文件中使用 `mount` |
| ✅ 在 `global.plugins` 中注入 `VueQueryPlugin` | 通过 | 测试文件中注入 |
| ✅ 抛出 Error 确保红灯 | 通过 | 占位文件均抛出 `Error` |
| ✅ 类型定义完整 | 通过 | `CapacityData` 接口完整 |

### 4.2 red灯阶段占位文件清单

| 文件 | 占位方式 | 状态 |
|------|----------|------|
| `types.ts` | `throw new Error('Red light phase only')` | ✅ |
| `index.ts` | `throw new Error('Red light phase only')` | ✅ |
| `queryClient.ts` | `const queryClient = null; throw Error(...)` | ✅ |
| `useCapacityQuery.ts` | `throw new Error('Red light phase only')` | ✅ |
| `capacity.ts` | `throw new Error('Red light phase only')` | ✅ |
| `queryClient.test.ts` | 无业务实现 | ✅ |
| `useCapacityQuery.test.ts` | 无业务实现 | ✅ |

---

## 🔴 五、V5 规约强制输出检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| ✅ 未编写任何业务代码 | 通过 | 占位文件抛出 Error |
| ✅ 类型定义完整 | 通过 | `CapacityData`, `UseQueryResult` |
| ✅ 测试用例对齐设计提案 | 通过 | 2 组 10 个核心用例 |
| ✅ 使用 mount 而非 renderHook | 通过 | 测试文件使用 `mount` |
| ✅ VueQueryPlugin 正确注入 | 通过 | `global.plugins: [VueQueryPlugin]` |
| ✅ 占位文件确保红灯 | 通过 | 所有文件抛出 Error |

---

> 测试代码已生成，已验证无业务实现代码时执行失败（红灯状态有效），测试用例完全对齐设计提案。请确认是否进入绿灯阶段编写业务实现代码？
