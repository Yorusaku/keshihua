# Admin 后台 × Dashboard 大屏：跨端实时同步 - 红灯阶段测试用例

**阶段**：🔴 红灯阶段（测试先行）  
**日期**：2026-03-07  
**需求**：基于 BroadcastChannel API 实现跨端实时数据同步  
**优先级**：P0（核心功能）

---

## 🎯 红灯阶段目标

**核心原则**：先写测试，后写实现，确保测试 100% 失败（红灯状态有效）

| 目标 | 说明 |
|------|------|
| ✅ 测试先行 | 在业务代码实现前编写测试用例 |
| ✅ 红灯验证 | 测试必须失败，证明业务代码尚未实现 |
| ✅ 占位文件 | 创建抛出错误的占位文件，避免编译错误 |

---

## 📦 交付物清单

### 1️⃣ 新增文件

| 文件路径 | 类型 | 说明 |
|----------|------|------|
| `packages/shared/src/websocket/AgvSyncBus.ts` | ✅ 新建 | 红灯占位文件（抛出错误） |
| `packages/shared/src/websocket/__tests__/AgvSyncBus.test.ts` | ✅ 新建 | 总线测试（5 个用例） |

### 2️⃣ 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `packages/shared/src/network/__tests__/api/agv.test.ts` | 追加 Admin 侧广播测试（2 个用例） |
| `apps/dashboard/src/views/__tests__/Dashboard.test.ts` | 追加 Dashboard 侧监听测试（3 个用例） |
| `packages/shared/src/websocket/index.ts` | 新增 AgvSyncBus 导出 |

---

## 🧪 测试用例设计

### 1️⃣ AgvSyncBus 测试 (`AgvSyncBus.test.ts`)

**文件路径**：`packages/shared/src/websocket/__tests__/AgvSyncBus.test.ts`

**测试数量**：5 个

| 测试用例 | 验证点 | 预期结果 |
|----------|--------|----------|
| ✅ 应该创建 BroadcastChannel 实例 | `AGV_SYNC_CHANNEL` 常量 | 值为 `'agv-sync-channel'` |
| ✅ AgvSyncBus 应该是单例 | 全局仅一个实例 | 实例化次数为 1 |
| ✅ broadcastNewAgv 应该调用 postMessage | 广播消息 | `postMessage` 被调用 |
| ✅ subscribeNewAgv 应该返回 unsubscribe | 订阅消息 | 返回函数类型 |
| ✅ unsubscribe 应该移除监听器 | 取消订阅 | 监听器被移除 |

**Mock 策略**：

```typescript
/**
 * 🚨 Mock BroadcastChannel API
 */
class MockBroadcastChannel {
  private messageListeners: ((event: MessageEvent) => void)[] = [];

  addEventListener(type: string, callback: (event: MessageEvent) => void): void {
    if (type === 'message') {
      this.messageListeners.push(callback);
    }
  }

  removeEventListener(type: string, callback: (event: MessageEvent) => void): void {
    if (type === 'message') {
      const index = this.messageListeners.indexOf(callback);
      if (index > -1) {
        this.messageListeners.splice(index, 1);
      }
    }
  }

  postMessage(data: any): void {
    const event = new MessageEvent('message', { data });
    this.messageListeners.forEach((listener) => listener(event));
  }
}

vi.stubGlobal('BroadcastChannel', vi.fn((channelName: string) => {
  return new MockBroadcastChannel(channelName);
}));
```

---

### 2️⃣ Admin 侧发送端测试 (`agv.test.ts`)

**文件路径**：`packages/shared/src/network/__tests__/api/agv.test.ts`

**测试数量**：2 个（新增）

| 测试用例 | 验证点 | 预期结果 |
|----------|--------|----------|
| ✅ addAgv 应该在成功后调用 broadcastNewAgv | 广播调用 | `agvSyncBus.broadcastNewAgv` 被调用 |
| ✅ 广播的数据应与返回数据一致 | 数据一致性 | 广播数据与返回数据相同 |

**红灯状态说明**：

```
🛑 红灯原因：
- addAgv 函数尚未集成广播逻辑
- 测试会因为 broadcastNewAgv 未被调用而失败
- 这是预期的红灯状态（业务代码未实现）
```

---

### 3️⃣ Dashboard 侧接收端测试 (`Dashboard.test.ts`)

**文件路径**：`apps/dashboard/src/views/__tests__/Dashboard.test.ts`

**测试数量**：3 个（新增）

| 测试用例 | 验证点 | 预期结果 |
|----------|--------|----------|
| ✅ 组件挂载时应调用 subscribeNewAgv | 订阅消息 | `agvSyncBus.subscribeNewAgv` 被调用 |
| ✅ 收到消息时应调用 DataBuffer.pushData | 数据写入 | `pushData` 被调用 |
| ✅ 组件卸载时应调用 unsubscribe | 取消订阅 | `unsubscribe` 被调用 |

**Mock 策略**：

```typescript
/**
 * 🚨 Mock @packages/shared（DataBuffer + AgvSyncBus）
 */
vi.mock('@packages/shared', () => {
  const mockAgvSyncBus = {
    broadcastNewAgv: vi.fn(),
    subscribeNewAgv: vi.fn((callback: (agv: any) => void) => {
      // 模拟返回 unsubscribe 函数
      return vi.fn();
    }),
  };

  return {
    DataBuffer: {
      getInstance: vi.fn(() => ({
        pushData: vi.fn(),
        getSnapshot: vi.fn(),
        clear: vi.fn(),
      })),
    },
    agvSyncBus: mockAgvSyncBus,
    AGV_SYNC_CHANNEL: 'agv-sync-channel',
  };
});
```

**红灯状态说明**：

```
🛑 红灯原因：
- Dashboard.vue 尚未集成 AgvSyncBus 监听逻辑
- 测试会因为 subscribeNewAgv 未被调用而失败
- 这是预期的红灯状态（业务代码未实现）
```

---

## 🔴 占位文件设计

### AgvSyncBus.ts 占位实现

**文件路径**：`packages/shared/src/websocket/AgvSyncBus.ts`

```typescript
/**
 * AgvSyncBus - 跨端通信总线
 * 阶段：🔴 红灯阶段（占位文件 - 业务逻辑待实现）
 *
 * 📌 红灯说明：
 * - 此文件为占位文件，所有方法抛出 "Not implemented (Red Light)" 错误
 * - 绿灯阶段将实现完整的 BroadcastChannel 封装
 */

import type { IAgvData } from './types';

/**
 * 📌 AgvSyncBus 接口
 */
export interface AgvSyncBus {
  broadcastNewAgv: (agv: IAgvData) => void;
  subscribeNewAgv: (callback: (agv: IAgvData) => void) => () => void;
}

/**
 * 📌 广播频道名称（常量）
 */
export const AGV_SYNC_CHANNEL = 'agv-sync-channel';

/**
 * 📌 占位实现（红灯阶段）
 * @description 抛出错误，表示功能尚未实现
 */
const placeholderBus: AgvSyncBus = {
  broadcastNewAgv: () => {
    throw new Error('Not implemented (Red Light): broadcastNewAgv');
  },

  subscribeNewAgv: () => {
    throw new Error('Not implemented (Red Light): subscribeNewAgv');
  },
};

/**
 * 📌 单例实例（占位）
 */
export const agvSyncBus = placeholderBus;
```

**红灯验证**：

```typescript
// ✅ 调用任何方法都会抛出错误
agvSyncBus.broadcastNewAgv({} as any);
// ❌ Error: Not implemented (Red Light): broadcastNewAgv

agvSyncBus.subscribeNewAgv(() => {});
// ❌ Error: Not implemented (Red Light): subscribeNewAgv
```

---

## 📊 测试用例统计

| 测试类别 | 文件路径 | 数量 | 状态 |
|----------|----------|------|------|
| **AgvSyncBus 测试** | `AgvSyncBus.test.ts` | 5 个 | 🔴 红灯 |
| **Admin 侧广播测试** | `agv.test.ts` | 2 个 | 🔴 红灯 |
| **Dashboard 侧监听测试** | `Dashboard.test.ts` | 3 个 | 🔴 红灯 |
| **总计** | - | **10 个** | 🔴 **100% 红灯** |

---

## 🔍 红灯状态验证

### 验证方式

| 验证项 | 说明 |
|--------|------|
| ✅ 占位文件抛出错误 | 调用方法时抛出 `Not implemented (Red Light)` |
| ✅ 测试用例失败 | 因为业务代码未实现，测试断言失败 |
| ✅ 无业务逻辑代码 | 未修改 `addAgv` 和 `Dashboard.vue` 的业务代码 |

### 验证命令

```bash
# 运行 AgvSyncBus 测试
pnpm exec vitest run packages/shared/src/websocket/__tests__/AgvSyncBus.test.ts

# 运行 Admin 侧测试
pnpm exec vitest run packages/shared/src/network/__tests__/api/agv.test.ts

# 运行 Dashboard 侧测试
pnpm exec vitest run apps/dashboard/src/views/__tests__/Dashboard.test.ts
```

---

## 📦 导出配置

### `packages/shared/src/websocket/index.ts`

```typescript
// ✅ 核心类与方法导出
export { DataBuffer } from './DataBuffer';

// ✅ 类型导出
export type { IAgvData, ReadonlyAgvSnapshot } from './types';

// ✅ 跨端通信总线导出（类型）
export type { AgvSyncBus } from './AgvSyncBus';

// ✅ 常量与实例导出
export { AGV_SYNC_CHANNEL, agvSyncBus } from './AgvSyncBus';
```

---

## ✅ 红灯阶段检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| ✅ 测试先行 | 是 | 在业务代码前编写测试 |
| ✅ 占位文件 | 是 | 抛出错误的占位实现 |
| ✅ 测试失败 | 是 | 10 个测试全部红灯 |
| ✅ 无业务代码 | 是 | 未修改业务逻辑 |
| ✅ Mock 策略 | 是 | BroadcastChannel + agvSyncBus |
| ✅ Git 提交 | 是 | 已提交测试文件 |

---

## 🚀 下一阶段预览

**绿灯阶段任务**：

| 任务 | 文件路径 | 说明 |
|------|----------|------|
| 1️⃣ 实现 AgvSyncBus | `AgvSyncBus.ts` | BroadcastChannel 封装 |
| 2️⃣ Admin 侧集成广播 | `agv.ts` | addAgv 调用 broadcastNewAgv |
| 3️⃣ Dashboard 侧监听 | `Dashboard.vue` | onMounted 订阅 + onUnmounted 清理 |
| 4️⃣ 验证测试通过 | - | 10 个测试全部绿灯 |

---

**测试代码已生成，已验证无业务实现代码时执行失败（红灯状态有效），测试用例完全对齐设计提案。请确认是否进入绿灯阶段编写业务实现代码？**