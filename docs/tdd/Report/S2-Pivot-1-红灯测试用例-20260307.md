# Admin 后台：产能透视报表 - 红灯阶段测试用例

**阶段**：🔴 红灯阶段（测试先行）  
**日期**：2026-03-07  
**需求**：基于 AntV S2 实现高性能虚拟表格  
**优先级**：P0（核心功能）

---

## 🎯 红灯阶段目标

**核心原则**：先写测试，后写实现，确保测试 100% 失败（红灯状态有效）

| 目标 | 说明 |
|------|------|
| ✅ 测试先行 | 在业务代码实现前编写测试用例 |
| ✅ 红灯验证 | 测试必须失败，证明业务代码尚未实现 |
| ✅ 占位文件 | 创建抛出错误的占位文件，避免编译错误 |

---

## 📦 交付物清单

### 1️⃣ 新增文件

| 文件路径 | 类型 | 说明 |
|----------|------|------|
| `packages/shared/src/network/api/report.ts` | ✅ 新建 | 红灯占位文件（抛出错误） |
| `packages/shared/src/network/__tests__/api/report.test.ts` | ✅ 新建 | Mock 数据测试（21 个用例） |
| `apps/admin/src/views/CapacityReport.vue` | ✅ 新建 | 占位组件（抛出错误） |
| `apps/admin/__tests__/views/CapacityReport.test.ts` | ✅ 新建 | S2 组件测试（8 个用例） |

### 2️⃣ 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `packages/shared/src/network/index.ts` | 新增 report API 导出 |

---

## 🧪 测试用例设计

### 1️⃣ Mock 数据测试 (`report.test.ts`)

**文件路径**：`packages/shared/src/network/__tests__/api/report.test.ts`

**测试数量**：21 个

| 测试类别 | 用例数 | 验证点 |
|----------|--------|--------|
| **Data Generation** | 3 | 生成数量（1000/500/2000） |
| **Data Structure** | 6 | required 字段（factory/line/date/yield/defectRate） |
| **Data Range** | 2 | yield (1000-11000), defectRate (0-0.1) |
| **Data Distribution** | 3 | factory/line/date 多样性 |
| **Async Filtering** | 6 | Promise/过滤/组合过滤 |
| **Response Structure** | 1 | required 字段 |
| **总计** | **21** | 21 个测试 |

**Mock 策略**：

```typescript
// ✅ 占位实现（红灯阶段）
const placeholderReport = {
  generateCapacityReportData: () => {
    throw new Error('Not implemented (Red Light): generateCapacityReportData');
  },
  fetchCapacityReport: () => {
    throw new Error('Not implemented (Red Light): fetchCapacityReport');
  },
};
```

**红灯状态**：

```
🛑 红灯原因：
- generateCapacityReportData 抛出 "Not implemented (Red Light)"
- fetchCapacityReport 抛出 "Not implemented (Red Light)"
- 所有测试因函数未实现而失败
```

---

### 2️⃣ S2 组件测试 (`CapacityReport.test.ts`)

**文件路径**：`apps/admin/__tests__/views/CapacityReport.test.ts`

**测试数量**：8 个

| 测试类别 | 用例数 | 验证点 |
|----------|--------|--------|
| **onMounted** | 3 | PivotSheet 实例化、render 调用、防御性检查 |
| **onBeforeUnmount** | 2 | destroy 调用、s2Instance 置空 |
| **Data Configuration** | 2 | s2DataConfig、s2Options 配置 |
| **Edge Cases** | 1 | 多次挂载/卸载 |
| **总计** | **8** | 8 个测试 |

**Mock 策略**：

```typescript
/**
 * 🚨 Mock @antv/s2（拦截 PivotSheet 类）
 */
vi.mock('@antv/s2', () => {
  return {
    PivotSheet: vi.fn((container, dataConfig, options) => {
      return {
        render: vi.fn(),
        destroy: vi.fn(),
        setDataCfg: vi.fn(),
        container,
        dataConfig,
        options,
      };
    }),
  };
});
```

**红灯状态**：

```
🛑 红灯原因：
- CapacityReport.vue 抛出 "Not implemented (Red Light)"
- 组件未实现 setup 逻辑
- PivotSheet 未被调用
```

---

## 🔴 占位文件设计

### report.ts 占位实现

**文件路径**：`packages/shared/src/network/api/report.ts`

```typescript
/**
 * 📌 占位实现（红灯阶段）
 * @description 抛出错误，表示功能尚未实现
 */
const placeholderReport = {
  generateCapacityReportData: () => {
    throw new Error('Not implemented (Red Light): generateCapacityReportData');
  },
  fetchCapacityReport: () => {
    throw new Error('Not implemented (Red Light): fetchCapacityReport');
  },
};

export const generateCapacityReportData = placeholderReport.generateCapacityReportData;
export const fetchCapacityReport = placeholderReport.fetchCapacityReport;
```

**红灯验证**：

```typescript
// ✅ 调用任何方法都会抛出错误
generateCapacityReportData(1000);
// ❌ Error: Not implemented (Red Light): generateCapacityReportData

fetchCapacityReport();
// ❌ Error: Not implemented (Red Light): fetchCapacityReport
```

### CapacityReport.vue 占位实现

**文件路径**：`apps/admin/src/views/CapacityReport.vue`

```vue
<script setup lang="ts">
/**
 * 📌 占位实现（红灯阶段）
 * @description 抛出错误，表示功能尚未实现
 */
throw new Error('Not implemented (Red Light): CapacityReport');
</script>
```

**红灯验证**：

```
🛑 组件加载时抛出错误
❌ Error: Not implemented (Red Light): CapacityReport
```

---

## 📊 测试用例统计

| 测试类别 | 文件路径 | 数量 | 状态 |
|----------|----------|------|------|
| **Mock 数据测试** | `report.test.ts` | 21 个 | 🔴 红灯 |
| **S2 组件测试** | `CapacityReport.test.ts` | 8 个 | 🔴 红灯 |
| **总计** | - | **29 个** | 🔴 **100% 红灯** |

---

## 🔍 红灯状态验证

### 验证方式

| 验证项 | 说明 |
|--------|------|
| ✅ 占位文件抛出错误 | 调用方法时抛出 `Not implemented (Red Light)` |
| ✅ 测试用例失败 | 因为业务代码未实现，测试断言失败 |
| ✅ 无业务逻辑代码 | 未修改业务逻辑 |
| ✅ Mock 策略 | PivotSheet 类被拦截 |

### 验证命令

```bash
# 运行 Mock 数据测试
pnpm exec vitest run packages/shared/src/network/__tests__/api/report.test.ts

# 运行 S2 组件测试
pnpm exec vitest run apps/admin/__tests__/views/CapacityReport.test.ts --environment jsdom
```

---

## 📦 导出配置

### `packages/shared/src/network/index.ts`

```typescript
// ✅ API 导出
export { fetchCapacityReport, ICapacityReportData } from './api/report';
```

---

## ✅ 红灯阶段检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| ✅ 测试先行 | 是 | 在业务代码前编写测试 |
| ✅ 占位文件 | 是 | 抛出错误的占位实现 |
| ✅ 测试失败 | 是 | 29 个测试全部红灯 |
| ✅ 无业务代码 | 是 | 未修改业务逻辑 |
| ✅ Mock 策略 | 是 | PivotSheet 类拦截 |
| ✅ JSDOM 兼容 | 是 | vi.mock 绕过 Canvas 崩溃 |
| ✅ Git 提交 | 是 | 已提交测试文件 |

---

## 🚀 下一阶段预览

**绿灯阶段任务**：

| 任务 | 文件路径 | 说明 |
|------|----------|------|
| 1️⃣ 实现 Mock 数据 | `report.ts` | generateCapacityReportData |
| 2️⃣ 实现 API | `report.ts` | fetchCapacityReport |
| 3️⃣ S2 封装 | `CapacityReport.vue` | PivotSheet + 生命周期 |
| 4️⃣ 验证测试通过 | - | 29 个测试全部绿灯 |

---

**测试代码已生成，已验证无业务实现代码时执行失败（红灯状态有效），测试用例完全对齐设计提案。请确认是否进入绿灯阶段编写业务实现代码？**
