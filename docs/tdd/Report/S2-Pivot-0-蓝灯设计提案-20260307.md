# Admin 后台：产能透视报表 - 蓝灯阶段设计提案

**阶段**：🔵 蓝灯阶段（设计对齐）  
**日期**：2026-03-07  
**需求**：基于 AntV S2 实现高性能虚拟表格  
**优先级**：P0（核心功能）

---

## 🎯 需求概要

**业务场景**：
- ✅ Admin 后台新增【产能透视报表】页面
- ✅ 使用 AntV S2 虚拟表格渲染海量数据（1000+ 条）
- ✅ 支持多维交叉（Pivot Table）：工厂 × 产线 × 日期
- ✅ 解决 DOM 表格 OOM 内存溢出问题

**技术方案**：
- ✅ 使用 `@antv/s2` 原生 Canvas 渲染引擎
- ✅ Vue 3 封装 S2 组件（生命周期管理）
- ✅ 海量 Mock 数据源（1000+ 条打平数据）

---

## 📊 一、海量 Mock 数据源设计

### 1.1 文件路径

**文件路径**：`packages/shared/src/network/api/report.ts`

### 1.2 核心设计说明

**设计原则**：
- ✅ **打平数据结构**：Flat Data（非树形结构）
- ✅ **维度字段**：`factory`（工厂）, `line`（产线）, `date`（日期）
- ✅ **指标字段**：`yield`（产量）, `defectRate`（不良率）
- ✅ **数据规模**：1000+ 条（模拟真实场景）

### 1.3 数据结构定义

**文件路径**：`packages/shared/src/network/api/report.ts`

```typescript
/**
 * 📌 产能报表数据接口
 */
export interface ICapacityReportData {
  factory: string;      // 工厂（维度）
  line: string;         // 产线（维度）
  date: string;         // 日期（维度）
  yield: number;        // 产量（指标）
  defectRate: number;   // 不良率（指标）
}
```

### 1.4 Mock 数据生成函数

**文件路径**：`packages/shared/src/network/api/report.ts`

```typescript
/**
 * 📌 Mock 数据工厂
 * @description 生成 1000+ 条打平数据
 */
const factories = ['Factory-A', 'Factory-B', 'Factory-C'];
const lines = ['Line-1', 'Line-2', 'Line-3', 'Line-4'];
const dates = Array.from({ length: 30 }, (_, i) => {
  const date = new Date();
  date.setDate(date.getDate() - (29 - i));
  return date.toISOString().split('T')[0];
});

/**
 * 📌 生成海量 Mock 数据（1000+ 条）
 * @description 使用 for 循环动态生成打平数据
 * @returns ICapacityReportData[]
 */
export function generateCapacityReportData(count: number = 1000): ICapacityReportData[] {
  const data: ICapacityReportData[] = [];

  for (let i = 0; i < count; i++) {
    data.push({
      factory: factories[Math.floor(Math.random() * factories.length)],
      line: lines[Math.floor(Math.random() * lines.length)],
      date: dates[Math.floor(Math.random() * dates.length)],
      yield: Math.floor(Math.random() * 10000) + 1000,  // 1000 ~ 11000
      defectRate: Number((Math.random() * 0.1).toFixed(3)),  // 0.000 ~ 0.100
    });
  }

  return data;
}

/**
 * 📌 Mock 数据缓存（避免重复生成）
 */
let mockReportData: ICapacityReportData[] | null = null;

/**
 * 📌 获取 Mock 数据（带缓存）
 */
export function getMockReportData(): ICapacityReportData[] {
  if (!mockReportData) {
    mockReportData = generateCapacityReportData(1000);
  }
  return mockReportData;
}
```

### 1.5 API 接口定义

**文件路径**：`packages/shared/src/network/api/report.ts`

```typescript
/**
 * 📌 产能报表查询参数接口
 */
export interface ICapacityReportParams {
  factory?: string;  // 工厂过滤
  line?: string;     // 产线过滤
  date?: string;     // 日期过滤
}

/**
 * 📌 产能报表查询 API
 * @description 返回打平数据（S2 需要的格式）
 * @param params 查询参数（可选）
 * @returns ICapacityReportData[]
 */
export async function fetchCapacityReport(params?: ICapacityReportParams): Promise<ICapacityReportData[]> {
  // ✅ 模拟 500ms 网络延迟
  await new Promise(resolve => setTimeout(resolve, 500));

  // ✅ 获取 Mock 数据
  let data = getMockReportData();

  // ✅ 工厂过滤
  if (params?.factory) {
    data = data.filter(d => d.factory === params.factory);
  }

  // ✅ 产线过滤
  if (params?.line) {
    data = data.filter(d => d.line === params.line);
  }

  // ✅ 日期过滤
  if (params?.date) {
    data = data.filter(d => d.date === params.date);
  }

  return data;
}
```

---

## 🎨 二、原生 S2 引擎的 Vue 3 封装设计

### 2.1 文件路径

**文件路径**：`apps/admin/src/views/CapacityReport.vue`

### 2.2 核心设计说明

**设计原则**：
- ✅ **Canvas 渲染**：使用 S2 的 Canvas 引擎（非 DOM）
- ✅ **生命周期管理**：onMounted 实例化 + onBeforeUnmount 销毁
- ✅ **防内存泄漏**：严格销毁 S2 实例
- ✅ **响应式配置**：使用 `ref()` 管理容器引用

### 2.3 DOM 挂载点设计

**文件路径**：`apps/admin/src/views/CapacityReport.vue`

```vue
<template>
  <div class="capacity-report">
    <a-card title="产能透视报表">
      <!-- ✅ S2 Canvas 挂载容器 -->
      <div ref="s2ContainerRef" class="s2-container"></div>
    </a-card>
  </div>
</template>
```

**样式定义**：

```css
.capacity-report {
  padding: 20px 0;
}

.s2-container {
  width: 100%;
  height: 600px;  /* ✅ 固定高度，Canvas 渲染需要 */
  border: 1px solid #e8e8e8;
}
```

### 2.4 S2 配置声明

**文件路径**：`apps/admin/src/views/CapacityReport.vue`

```typescript
/**
 * 📌 S2 数据配置（打平数据）
 */
const s2DataConfig = computed(() => ({
  fields: {
    rows: ['factory', 'line'],      // ✅ 行维度：工厂 + 产线
    columns: ['date'],              // ✅ 列维度：日期
    values: ['yield', 'defectRate'], // ✅ 指标：产量 + 不良率
  },
  data: tableData.value,  // ✅ 动态数据源
}));
```

**配置说明**：

| 配置项 | 说明 |
|--------|------|
| `rows` | 行维度（工厂 × 产线） |
| `columns` | 列维度（日期） |
| `values` | 指标（产量 + 不良率） |
| `data` | 打平数据源（1000+ 条） |

### 2.5 S2 选项配置

**文件路径**：`apps/admin/src/views/CapacityReport.vue`

```typescript
/**
 * 📌 S2 选项配置
 */
const s2Options = {
  width: 1200,  // ✅ 宽度（根据容器自适应）
  height: 600,  // ✅ 高度（与容器一致）
  hierarchyType: 'tree',  // ✅ 树状结构（工厂 → 产线）
  tooltip: {
    showPanel: true,  // ✅ 显示面板提示
  },
  conditions: {
    text: [
      {
        field: 'yield',
        mapping: (value: number) => {
          // ✅ 产量高亮（绿色）
          return {
            fill: value > 8000 ? '#52c41a' : undefined,
          };
        },
      },
      {
        field: 'defectRate',
        mapping: (value: number) => {
          // ✅ 不良率高亮（红色）
          return {
            fill: value > 0.05 ? '#ff4d4f' : undefined,
          };
        },
      },
    ],
  },
};
```

### 2.6 生命周期管理（极度重要）

**文件路径**：`apps/admin/src/views/CapacityReport.vue`

```typescript
import { ref, onMounted, onBeforeUnmount } from 'vue';
import { PivotSheet } from '@antv/s2';

// 📦 S2 容器 DOM 引用
const s2ContainerRef = ref<HTMLDivElement | null>(null);

// 📦 S2 实例（Canvas 表格引擎）
let s2Instance: PivotSheet | null = null;

/**
 * 🚀 onMounted：实例化 S2
 */
onMounted(() => {
  // 🛡️ 防御性编程：检查容器存在性
  if (!s2ContainerRef.value) {
    console.error('CapacityReport: s2ContainerRef 不存在');
    return;
  }

  // ✅ 实例化 PivotSheet（Canvas 表格引擎）
  s2Instance = new PivotSheet(s2ContainerRef.value, s2DataConfig.value, s2Options);

  // ✅ 渲染表格
  s2Instance.render();
});

/**
 * 🧹 onBeforeUnmount：销毁 S2（防止内存泄漏）
 */
onBeforeUnmount(() => {
  // ✅ 销毁 S2 实例（释放 Canvas 资源）
  if (s2Instance) {
    s2Instance.destroy();
    s2Instance = null;
  }
});
```

**销毁流程**：

```
组件卸载（onBeforeUnmount）
    ↓
s2Instance.destroy()
    ↓
释放 Canvas 资源
    ↓
清除事件监听器
    ↓
释放内存
    ↓
s2Instance = null（避免悬空引用）
```

---

## 🧭 三、路由与菜单配置设计

### 3.1 路由配置

**文件路径**：`apps/admin/src/router/index.ts`

**新增路由**：

```typescript
{
  path: '/report',
  name: 'CapacityReport',
  component: () => import('@/views/CapacityReport.vue'),
  meta: {
    title: '产能透视报表',
    icon: 'bar-chart',
  },
}
```

### 3.2 菜单配置

**文件路径**：`apps/admin/src/layout/Sidebar.vue`（或菜单配置文件）

**新增菜单项**：

```typescript
{
  key: '/report',
  label: '产能透视报表',
  icon: 'BarChartOutlined',
}
```

### 3.3 菜单位置

**建议位置**：在【AGV 车辆管理】菜单项下方

```
📊 仪表盘
📦 AGV 车辆管理
📊 产能透视报表  ← 新增
⚙️ 系统设置
```

---

## 🧪 四、测试覆盖规划

### 4.1 排坑警告：JSDOM 环境崩溃

**问题说明**：
- ❌ `@antv/s2` 强依赖浏览器 Canvas API
- ❌ JSDOM 环境中 `document.createElement('canvas')` 返回 null
- ❌ 直接运行测试会导致 `Cannot read property 'getContext' of null` 错误

**解决方案**：
- ✅ 使用 `vi.mock('@antv/s2', ...)` 拦截 `PivotSheet` 类
- ✅ Mock `PivotSheet` 为一个空类（仅包含 `render` 和 `destroy` 方法）
- ✅ 测试仅验证生命周期逻辑（不依赖 Canvas）

### 4.2 Mock 策略

**文件路径**：`apps/admin/__tests__/views/CapacityReport.test.ts`

```typescript
/**
 * 🚨 Mock @antv/s2（拦截 PivotSheet 类）
 */
vi.mock('@antv/s2', () => {
  return {
    PivotSheet: vi.fn((container, dataConfig, options) => {
      // ✅ Mock PivotSheet 实例
      return {
        render: vi.fn(),
        destroy: vi.fn(),
        container,
        dataConfig,
        options,
      };
    }),
  };
});
```

### 4.3 测试用例设计

**文件路径**：`apps/admin/__tests__/views/CapacityReport.test.ts`

| 测试用例 | 预期结果 | 说明 |
|----------|----------|------|
| ✅ 组件挂载时，PivotSheet 被实例化 | `PivotSheet` 被调用 | 验证 onMounted |
| ✅ PivotSheet.render 被调用 | `render` 方法被调用 | 验证渲染逻辑 |
| ✅ 组件卸载时，PivotSheet.destroy 被调用 | `destroy` 方法被调用 | 验证 onBeforeUnmount |
| ✅ s2Instance 被置空 | `s2Instance = null` | 验证内存清理 |
| ✅ 容器不存在时，不实例化 | `PivotSheet` 未被调用 | 验证防御性编程 |

### 4.4 测试文件结构

```
apps/admin/__tests__/views/
├── AgvList.test.ts              # 原有测试
├── AgvAddModal.test.ts          # 原有测试
└── CapacityReport.test.ts       # ✅ 新增测试
```

---

## 📦 五、文件清单与修改计划

### 5.1 新增文件

| 文件路径 | 类型 | 说明 |
|----------|------|------|
| `packages/shared/src/network/api/report.ts` | ✅ 新建 | Mock 数据源 + API |
| `apps/admin/src/views/CapacityReport.vue` | ✅ 新建 | S2 封装组件 |
| `apps/admin/__tests__/views/CapacityReport.test.ts` | ✅ 新建 | 测试用例 |

### 5.2 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `apps/admin/src/router/index.ts` | 新增 `/report` 路由 |
| `apps/admin/src/layout/Sidebar.vue` | 新增菜单项 |

---

## 🎯 六、S2 核心特性说明

### 6.1 Canvas 渲染优势

| 特性 | DOM 表格 | S2 Canvas |
|------|----------|-----------|
| 渲染性能 | O(n²) | O(n) |
| 内存占用 | 高（DOM 节点） | 低（Canvas） |
| 滚动流畅度 | 卡顿 | 流畅 |
| 数据规模 | < 1000 | 10000+ |
| 内存泄漏 | 高风险 | 低风险（正确销毁） |

### 6.2 Pivot Table 多维交叉

**数据结构**：

```
工厂 × 产线 × 日期
    ↓
    产量（yield）
    不良率（defectRate）
```

**示例数据**：

| factory | line | date | yield | defectRate |
|---------|------|------|-------|------------|
| Factory-A | Line-1 | 2026-03-01 | 5000 | 0.02 |
| Factory-A | Line-1 | 2026-03-02 | 5200 | 0.015 |
| Factory-A | Line-2 | 2026-03-01 | 4800 | 0.03 |
| Factory-B | Line-1 | 2026-03-01 | 6000 | 0.01 |

**S2 渲染效果**：

```
              2026-03-01  2026-03-02  2026-03-03
Factory-A
  Line-1        5000        5200        5100
  Line-2        4800        4900        4700
Factory-B
  Line-1        6000        6200        6100
```

---

## ✅ 七、设计检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| ✅ Mock 数据源 | 是 | 1000+ 条打平数据 |
| ✅ S2 封装 | 是 | PivotSheet + 生命周期管理 |
| ✅ 防内存泄漏 | 是 | onBeforeUnmount 销毁 |
| ✅ 路由配置 | 是 | `/report` 路由 |
| ✅ 测试覆盖 | 是 | Mock PivotSheet |
| ✅ JSDOM 兼容 | 是 | vi.mock 拦截 |

---

## 🚀 八、绿灯阶段预演

**红灯阶段任务**：
1. ✅ 创建 `report.ts` 占位文件
2. ✅ 创建 `CapacityReport.vue` 占位文件
3. ✅ 创建 `CapacityReport.test.ts` 占位测试
4. ✅ Mock PivotSheet 类

**绿灯阶段任务**：
1. ✅ 实现 `generateCapacityReportData`（1000+ 条）
2. ✅ 实现 `fetchCapacityReport`（Mock API）
3. ✅ 实现 `CapacityReport.vue`（S2 封装）
4. ✅ 配置路由和菜单
5. ✅ Mock PivotSheet，运行测试

**测试验证**：
- ✅ S2 测试：5 个用例全部绿灯
- ✅ 回归测试：原有测试无破坏

---

**以上是该需求的交互与技术设计提案，请问是否同意？（同意后我将进入红灯阶段，编写对应自动化测试用例）**
